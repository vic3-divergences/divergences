dvg_embrace_armenians = {
	is_shown = {
		has_variable = armenian_accepted
		NOT = { has_law = law_type:law_state_religion }
	}

    possible = {
		has_variable = armenian_accepted
		NOT = { has_law = law_type:law_state_religion }
		
	}

    when_taken = {
		remove_variable = armenian_accepted
		add_primary_culture = cu:armenian
    }

	ai_chance = {
		base = 100
	}
}

dvg_armenian_liberation = {
	is_shown = {
		
	country_has_primary_culture = cu:armenian
		
	NOT = { has_global_variable = armenia_claims }	
	}
    possible = {
		has_technology_researched = nationalism
		country_rank > rank_value:insignificant_power
	}
    when_taken = {
		every_state_region = {
			limit = {
			OR = {
				is_homeland = cu:armenian
				}
			}
			add_claim = ROOT
		}
		create_incident = {
			country = ROOT
			value = 15
		}
		set_global_variable = {
				name = armenia_claims
				value = 0
		}
    }

	ai_chance = {
		base = 100
	}
}

dvg_embrace_assyrians = {
	is_shown = {
		OR = {
		c:KU1 = ROOT 
		AND = {
		country_has_primary_culture = cu:kurdish
		was_formed_from = KU1
		}
		}
		NOT = { country_has_primary_culture = cu:assyrian}
	}

    possible = {
		NOT = { has_law = law_type:law_ethnostate }
		NOT = { has_law = law_type:law_state_religion }
		cu:assyrian = {
			NOT = { culture_is_discriminated_in = ROOT }
		}
		
		
	}

    when_taken = {
		remove_modifier = dvg_soyurghal
    }

	ai_chance = {
		base = 100
	}
}

dvg_abolish_soyurghal = {
	is_shown = {
		NOT = { has_law = law_type:law_traditionalism }
		has_law = law_type:appointed_bureaucrats
	}

    possible = {
		NOT = { has_law = law_type:law_traditionalism }
		has_law = law_type:appointed_bureaucrats
		has_technology_researched = currency_standards
	}

    when_taken = {
		remove_variable = armenian_accepted
		add_primary_culture = cu:armenian
    }

	ai_chance = {
		base = 100
	}
}

demand_qeshm = {
	is_shown = {
		country_rank = rank_value:great_power
		NOT = { has_global_variable = qeshm_demanded }
	}

    possible = {
		country_rank = rank_value:great_power
		has_interest_marker_in_region = region_persia
		has_interest_marker_in_region = region_arabic
		NOT = { has_truce_with = c:SHZ 
		owns_entire_state_region = STATE_LARISTAN
		navy_size >= 20
		}
		c:SHZ = {
				owns_entire_state_region = STATE_LARISTAN
			}
	}

    when_taken = {
		c:SHZ = {
			trigger_event = { id = dvg_persia.7 days = 10 }
		}
		set_global_variable = qeshm_demanded
    }

	ai_chance = {
		base = 100
	}
}

dvg_annex_turkmen = {
	is_shown = {
	
	    exists = c:TDT

		country_has_primary_culture = cu:turkmen
		
		NOT = {
			owns_entire_state_region = STATE_TURKMENIA
		}
		
	}

    possible = {
		OR = {
		has_state_in_state_region = STATE_TURKMENIA
		}
		has_technology_researched = nationalism
		has_technology_researched = colonization
		NOT = { has_law = law_type:law_no_colonial_affairs }
	
	}

    when_taken = {
		s:STATE_TURKMENIA.region_state:TDT = {
			set_state_owner = ROOT
		}
    }

	ai_chance = {
		base = 100
	}
}