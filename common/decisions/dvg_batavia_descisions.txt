dvg_form_batavia = {
	is_shown = {
		NOT = {
			exists = c:BTV
		}
		OR = {
			c:NFL
			c:OPS
			c:LEW
		}
		has_technology_researched = nationalism
	}


    possible = {
		root = {
			AND = {
				owns_entire_state_region = STATE_TRANS_ORANJE
				OR = {
					owns_entire_state_region = STATE_NORTH_CAPE
					c:LEW = {
						OR = {
							is_subject_of = root
							has_diplomatic_pact = {
								who = ROOT
								type = customs_union
							}
						}	
					}
				}
				OR = {
					AND = {
					owns_entire_state_region = STATE_TRANSVAAL
					owns_entire_state_region = STATE_VRYSTAAT
					}
					c:OPS = {
						OR = {
							is_subject_of = root
							has_diplomatic_pact = {
								who = ROOT
								type = customs_union
							}
						}	
					}
				}
				OR = {
					owns_entire_state_region = STATE_LOURENCO_MARQUES
					c:NFL = {
						OR = {
							is_subject_of = root
							has_diplomatic_pact = {
								who = ROOT
								type = customs_union
							}
						}	
					}
				}
			}
		}
	}

    when_taken = {
    	c:root = {
    		change_tag = BTV
    	}
    	hidden_effect = {
    		annex = LEW
    		annex = OPS
    		annex = NFL
    	}
		trigger_event = { id = dvg_batavia.1 popup = yes }
    }

	ai_chance = {
		base = 100
	}
}