strategic_region_stance_type = {
	icon = "texture.dds"

	can_send_fleets = <yes/no>
}

- Add new stance files in this folder using the same structure.
- Localization keys should match the stance keys (e.g. strategic_region_stance_none) if UI text is later required.