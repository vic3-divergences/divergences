law_hereditary_bureaucrats = {
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/hereditary_bureaucrats.dds"

	progressiveness = -50

	is_visible = {
		NOR = {
			c:AUS ?= this # For the Austrian Empire and its subjects
			AND = {
				exists = c:AUS
				is_subject_of = c:AUS
			}
			c:DUA ?= this # For the Dual Monarquie and its subjects
			AND = {
				exists = c:DUA
				is_subject_of = c:DUA
				OR = {
					root.capital.region ?= sr:region_france
					root.capital.region ?= sr:region_england
				}
			}
		}
	}

	modifier = {
		state_bureaucracy_population_base_cost_factor_mult = -0.25
		country_aristocrats_pol_str_mult = 0.25
	}

	on_enact = {
		custom_tooltip = urban_aristocrats_int_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	disallowing_laws = {
		law_council_republic
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_traditionalist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_crownland_diets = {
	group = lawgroup_bureaucracy
	parent = law_hereditary_bureaucrats # Inherits ideological stances from parent

	icon = "gfx/interface/icons/law_icons/crownland_diets.dds"

	progressiveness = -50

	is_visible = {
		OR = {
			c:AUS ?= this # For the Austrian Empire and its subjects
			AND = {
				exists = c:AUS
				is_subject_of = c:AUS
			}
		}
	}

	modifier = {
		state_bureaucracy_population_base_cost_factor_mult = -0.25
		country_aristocrats_pol_str_mult = 0.10
	}

	on_enact = {
		custom_tooltip = urban_aristocrats_int_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	disallowing_laws = {
		law_council_republic
	}

	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_traditionalist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_states_general = {
	group = lawgroup_bureaucracy
	parent = law_hereditary_bureaucrats # Inherits ideological stances from parent

	icon = "gfx/interface/icons/law_icons/crownland_diets.dds"

	progressiveness = -90

	is_visible = {
		OR = {
			c:DUA ?= this # For the Dual Monarquie and its subjects
			AND = {
				exists = c:DUA
				is_subject_of = c:DUA
				OR = {
					root.capital.region ?= sr:region_france
					root.capital.region ?= sr:region_england
				}
			}
		}
	}

	modifier = {
		state_bureaucracy_population_base_cost_factor_mult = -0.15
		country_aristocrats_pol_str_mult = 0.35
	}

	on_enact = {
		custom_tooltip = urban_aristocrats_int_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
	}

	disallowing_laws = {
		law_council_republic
	}


	ai_will_do = {
		exists = ruler
		ruler = {
			has_ideology = ideology:ideology_traditionalist
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_appointed_bureaucrats = {
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/appointed_bureaucrats.dds"

	progressiveness = 0

	is_visible = {
		NOR = {
			c:DUA ?= this # For the Dual Monarquie and its subjects
			AND = {
				exists = c:DUA
				is_subject_of = c:DUA
				OR = {
					root.capital.region ?= sr:region_france
					root.capital.region ?= sr:region_england
				}
			}
		}
	}
	modifier = {
		country_bureaucrats_pol_str_mult = 0.25
		state_tax_capacity_mult = 0.25
	}

	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_general_office = {
	group = lawgroup_bureaucracy
	parent = law_appointed_bureaucrats # Inherits ideological stances from parent

	icon = "gfx/interface/icons/law_icons/appointed_bureaucrats.dds"

	progressiveness = 10

	is_visible = {
		OR = {
			c:DUA ?= this # For the Dual Monarquie and its subjects
			AND = {
				exists = c:DUA
				is_subject_of = c:DUA
				OR = {
					root.capital.region ?= sr:region_france
					root.capital.region ?= sr:region_england
				}
			}
		}
		NOT = {
            has_law = law_type:law_appointed_bureaucrats
        }
	}

	modifier = {
		country_bureaucrats_pol_str_mult = 0.25
		state_tax_capacity_mult = 0.25
	}

	on_enact = {
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = bureaucrats_pb_ig_shift_effect_negative_tt
		}
		if = {
			limit = {
				has_law_or_variant = law_type:law_appointed_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_elected_bureaucrats = {
	group = lawgroup_bureaucracy

	icon = "gfx/interface/icons/law_icons/elected_bureaucrats.dds"

	progressiveness = 50

	is_visible = {
		NOR = {
			c:DUA ?= this # For the Dual Monarquie and its subjects
			AND = {
				exists = c:DUA
				is_subject_of = c:DUA
				OR = {
					root.capital.region ?= sr:region_france
					root.capital.region ?= sr:region_england
				}
			}
		}
	}
	disallowing_laws = {
		law_technocracy
		law_elder_council
	}

	on_enact = {
		custom_tooltip = bureaucrats_pb_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_hereditary_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	modifier = {
		country_bureaucracy_investment_cost_factor_mult = -0.15
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_provincial_councils = {
	group = lawgroup_bureaucracy
	parent = law_elected_bureaucrats # Inherits ideological stances from parent

	icon = "gfx/interface/icons/law_icons/elected_bureaucrats.dds"

	progressiveness = 90

	is_visible = {
		OR = {
			c:DUA ?= this # For the Dual Monarquie and its subjects
			AND = {
				exists = c:DUA
				is_subject_of = c:DUA
				OR = {
					root.capital.region ?= sr:region_france
					root.capital.region ?= sr:region_england
				}
			}
		}
		NOT = {
            has_law = law_type:law_elected_bureaucrats
        }
	}

	disallowing_laws = {
		law_elder_council
	}

	on_enact = {
		custom_tooltip = bureaucrats_pb_ig_shift_effect_tt
		if = {
			limit = {
				has_law_or_variant = law_type:law_elected_bureaucrats
			}
			custom_tooltip = urban_aristocrats_int_ig_shift_effect_negative_tt
		}
	}

	modifier = {
		country_bureaucracy_investment_cost_factor_mult = -0.20
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}
